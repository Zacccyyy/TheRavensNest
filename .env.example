# Copy this file to .env (which is gitignored) and fill in your key.
# Only needed for photo identification — everything else works without it.
# Get a key at https://platform.claude.com/
ANTHROPIC_API_KEY=
